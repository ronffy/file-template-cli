import { useEffect } from 'react'
import { use{{__Name}}Store } from '@/store/{{__name}}'

interface Props {}

export default function {{__Name}}(props: Props) {
  return (
    <div gap={16}>
      xx
    </div>
  )
}
